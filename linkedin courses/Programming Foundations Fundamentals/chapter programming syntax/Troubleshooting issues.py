# syntax error when we remove one of the semicolone
print("Hello world")

# runtime error: because division by zero will give error but this will appear during runtime
10 * (2/0)

# semantic error: unexpected output
name = "Alice"
print("Hello name")
